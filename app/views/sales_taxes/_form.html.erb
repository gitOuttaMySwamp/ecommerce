<%= form_with(model: sales_tax, local: true) do |form| %>
  <% if sales_tax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sales_tax.errors.count, "error") %> prohibited this sales_tax from being saved:</h2>

      <ul>
        <% sales_tax.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
